# コードベース棚卸し・実態調査レポート

**作成日**: 2026-06-12
**調査範囲**: app/ / components/ / lib/ / types/ / hooks/ / contexts/ / scripts/ / supabase/ + Supabase実DB（読み取り専用）
**目的**: ドキュメントと実コードの乖離、未使用・重複コードの洗い出し
**注意**: Supabaseは**読み取り専用で確認のみ**（変更・マイグレーション実行は一切していない）

---

## 0. サマリ（要対応の優先度順）

| 優先 | 種別 | 内容 |
|------|------|------|
| 🔴 | 重複 | **スタンプ付与エンドポイントが3系統**（`/api/stamps`・`/api/stamps/scan`・`/api/stamps/auto`）でロジック重複・仕様差 |
| 🔴 | 乖離 | **repoのマイグレーションと実DBが不一致**（`026`・`030`欠番、`message_delivery_logs`はrepo外で作成） |
| 🟡 | 未使用 | `/api/rewards`(GET)・`/api/families/members`(bare DELETE) がフロントから呼ばれない |
| 🟡 | 未使用 | コンポーネント `ToothDiagram.tsx`(v1)・`KidsSlotButton.tsx` が未import |
| 🟡 | 未使用 | `lib/analytics.ts` のデッドコード（`logRewardExchange`等、§60参照） |
| 🟡 | 重複 | ケア記録テーブルが2つ：実体は **`patient_dental_records`（7行）**、`dental_records`（空）は重複した別テーブル |
| 🟢 | 空テーブル | `family_members`・`survey_questions`・`survey_responses`・`dental_records`(空・重複)・`message_delivery_logs`・`event_logs_daily_summary` は存在するが空（未活用） |
| 🟢 | 整理 | ルート直下の調査用 `.ts` 4本、`scripts/` の一回限り調査スクリプト多数 |

---

## 1. API Routes 棚卸し（全23ルート）

### 1.1 使用中（フロントから呼ばれている）

| ルート | 呼び出し元 | 用途 |
|--------|-----------|------|
| `POST /api/stamps` | [AdultHome.tsx:426](../components/(adult)/AdultHome.tsx#L426)（`addStamp`） | **会員証画面のQRScanner**（qrCodeId方式・旧来） |
| `POST /api/stamps/scan` | [scan/page.tsx](../app/scan/page.tsx) | **/scanページ**のアプリ内スキャン（payload方式） |
| `POST /api/stamps/auto` | [auto-stamp/page.tsx](../app/auto-stamp/page.tsx) | カメラQR直接起動（action param方式） |
| `POST /api/stamps/scan/delete-today` | [StaffPinModal.tsx](../components/shared/StaffPinModal.tsx) | スタッフ：本日QR削除 |
| `POST /api/stamps/slot` | [slot/page.tsx](../app/slot/page.tsx) | スロット当選付与 |
| `POST /api/stamps/manual` | [AdultHome](../components/(adult)/AdultHome.tsx) / [KidsStampPage](../components/(kids)/KidsStampPage.tsx) | スタッフ手動付与 |
| `POST /api/slot/unlock` | [KidsStampPage](../components/(kids)/KidsStampPage.tsx) | スロット解放 |
| `POST /api/rewards/exchange` | [lib/rewards.ts](../lib/rewards.ts) | 特典交換 |
| `GET /api/users/me` | settings / family/create / family/join | ユーザー情報 |
| `POST /api/users/setup-role` | onboarding | 親子ロール設定 |
| `GET|PUT /api/users/[userId]/memo` | [lib/memo.ts](../lib/memo.ts) | 次回メモ |
| `POST /api/users/[userId]/reservation-click` | [AdultHome.tsx:201](../components/(adult)/AdultHome.tsx#L201) | 予約クリック計測 |
| `GET|PATCH /api/profiles/[profileId]` | child-mode / child-mode/settings | プロフィール操作 |
| `POST /api/families/create` | family/create | 家族作成 |
| `POST /api/families/join` | family/join | 家族参加 |
| `GET /api/families/me` | settings / family/manage | 家族情報 |
| `PATCH /api/families/update` | family/manage | 家族名更新 |
| `POST /api/families/members/add` | family/manage | 代理メンバー追加 |
| `PATCH|DELETE /api/families/members/[memberId]` | family/manage | メンバー更新/削除 |
| `POST /api/survey/check` | AppLayout / survey | アンケート表示判定 |
| `POST /api/survey/submit` | survey | 回答送信 |
| `POST /api/survey/postpone` | AppLayout | 後回し |

### 1.2 未使用（フロントから呼び出し0件）

| ルート | 状況 |
|--------|------|
| `GET /api/rewards` | ❌ フロントは `milestone_rewards` を直接SELECT（[lib/rewards.ts](../lib/rewards.ts) `fetchMilestoneRewards`）。このGETルートは未使用＝旧仕様の残骸 |
| `DELETE /api/families/members`（bare [route.ts](../app/api/families/members/route.ts)） | ❌ 実際の削除は `members/[memberId]` 経由。bareルートは未使用＝[memberId]版に置換済み |

### 1.3 🔴 重複・整合性の問題：スタンプ付与が3系統

スタンプを付与するAPIが**3つ独立に存在**し、それぞれが「重複チェック・マイルストーン付与・イベントログ」を**個別実装**している。

| ルート | 入力方式 | 1日1回制限 | 5秒重複防止 | イベントログ | 備考 |
|--------|---------|-----------|------------|------------|------|
| `/api/stamps` | `qrCodeId`（QR生値） | あり | （要確認） | success/fail | **会員証画面のQRScanner**用・旧来 |
| `/api/stamps/scan` | `{type, stamps, qrCodeId}` | あり | あり | success_in_app/fail | /scanページ用 |
| `/api/stamps/auto` | `{amount, type, location}` | あり | あり | success(camera)/fail | カメラQR用 |

**問題点**:
- 入力フォーマットがバラバラ（qrCodeId / payload / action param）で、同じ「来院スタンプ」を3経路で付与している。
- ロジック（重複判定の方法・JST境界・マイルストーン呼び出し）が各ファイルにコピーされており、**修正漏れ・仕様差の温床**（過去の「15ポイント問題」「二重付与」もこの分散が一因）。
- さらに**アプリ内スキャンUIが2つ**ある：会員証画面の `QRScanner`(→`/api/stamps`) と 専用 `/scan`ページ(→`/api/stamps/scan`)。現状アプリ内スキャンは一時無効化（[123](123_アプリ内スキャン機能_一時無効化.md)）。

> 改善方向（提案・本レポートでは変更しない）：付与ロジックを1つの内部関数に集約し、3ルートは入力アダプタとして薄くする。ただし運用中のため段階的に。

---

## 2. ページ / コンポーネント 棚卸し

### 2.1 未使用コンポーネント

| ファイル | 状況 |
|---------|------|
| [components/dental/ToothDiagram.tsx](../components/dental/ToothDiagram.tsx)（v1） | ❌ 未使用。ケア記録の両ページとも `ToothDiagram_v2` をimport（`import ToothDiagram from '.../ToothDiagram_v2'`）。**v1は完全な死蔵** |
| [components/shared/KidsSlotButton.tsx](../components/shared/KidsSlotButton.tsx) | ❌ 未使用。[AppLayout.tsx:19](../components/layout/AppLayout.tsx#L19) に「スタンプタブに統合済み」とコメントがあり、importも撤去済み |

### 2.2 使用中の主要コンポーネント（参考）
`AppLayout` / `(adult)・(kids)` 各ページ / `QRScanner` / `StaffPinModal` / `VersionInfo` / `BirthMonthPromptModal` / `FriendshipPromptModal` / `SurveyModal`・`SurveyForm`・`SurveyCompleted`・`StarRating`・`NPSScale`（後2つはSurveyForm内で使用） / `AddChildDialog` / `ToothDiagram_v2`・`TreatmentTimeline`・`Legend`(dental) / `ExternalBrowserWarning`

### 2.3 注記
- `dental/Legend.tsx` はケア記録で使用。ただし `dental_records` テーブルが空（§4）のため、ケア記録機能は**実データ未投入の状態**で画面だけ存在。

---

## 3. lib / types / hooks / contexts

### 3.1 デッドコード（定義のみ・未使用）
[lib/analytics.ts](../lib/analytics.ts)：`logPageView` / `logRewardExchange` / `logError` / `logChildScreenView` / `startSession`（+ `endSession`は実質無効）。詳細と影響は [60_イベントログ設計.md](60_イベントログ設計.md) §6 を参照。

### 3.2 ルート直下の調査用ファイル（scripts/へ移動 or 削除候補）
リポジトリ直下に紛れている一回限りの調査スクリプト：
- `check_notes_location.ts` / `check_schema.ts` / `check_stamp_method.ts` / `investigate-kakiya-duplicate.ts`

→ いずれも本番コードからは参照されない。`scripts/` へ集約するか削除推奨。

---

## 4. Supabase 実DB（読み取り専用確認・2026-06-12）

> `scripts/introspect-readonly.mjs` でSELECT/HEADのみ実行。

| テーブル/ビュー | 行数 | 状態・備考 |
|----------------|------|-----------|
| `profiles` | 465 | 20カラム（line_user_id, ticket_number, view_mode, family_id, family_role, real_name, birth_month, visit_count, reservation_button_clicks 等） |
| `stamp_history` | 548 | 10カラム |
| `reward_exchanges` | 706 | milestone_reached, valid_until, is_first_time, is_milestone_based 等 |
| `milestone_rewards` | 3 | **特典マスタの実体（フロントはこれを使用）** |
| `milestone_history` | 436 | マイルストーン到達履歴（稼働中） |
| `rewards` | 4 | ⚠️ 旧特典マスタ。フロントは未使用（`/api/rewards`GETも未使用） |
| `event_logs` | 4400 | 6カラム |
| `families` | 15 | - |
| `family_stamp_totals` | 15 | ビュー（家族合算） |
| `staff` | 20 | login_id, password_hash 等（ダッシュボード用） |
| `activity_logs` | 1172 | ダッシュボード側の操作監査ログ（ミニアプリは書かない） |
| `surveys` | 1 | - |
| `family_members` | **空** | ⚠️ 家族メンバーは `profiles.family_id` で管理。このテーブルは未活用 |
| `survey_questions` | **空** | ⚠️ SurveyFormは設問ハードコード。テーブル未活用 |
| `survey_responses` | **空** | ⚠️ 回答は別経路。テーブル未活用 |
| `patient_dental_records` | 7 | **ケア記録の実体（データあり・稼働中）**。`019`で作成 |
| `dental_records` | **空** | ⚠️ 重複した別テーブル（空）。本物は `patient_dental_records`。命名重複＝整理候補 |
| `message_delivery_logs` | **空（存在）** | ⚠️ repoにマイグレーション無し＝**Supabase側で直接作成**された。空 |
| `event_logs_daily_summary` | **空（存在）** | サマリ未運用 |
| `daily_active_users` / `event_summary` | 88 / 7 | ビュー（稼働中） |

### 4.1 🔴 repoマイグレーション と 実DB の不一致
- 実DBに存在するが repo に**マイグレーションファイルが無い**もの：`message_delivery_logs`（Supabase上で直接作成された痕跡）。
- repoのマイグレーション番号に**欠番**：`016`(016Bのみ)・`026`・`030`。特に `026_minimal_rls_hardening` は [031の冒頭コメント](../supabase/031_fix_event_logs_rls.sql) で参照されているが**ファイルが存在しない**。
- **重複番号**：`009_`（add_family_support と fix_rls_policies）、`021_`（fix_visit_count と milestone_rewards_migration）。
- event_logs のRLSは3回改訂（`015`→`031`→`032`）。`015`のRLSは現状と異なる（[60](60_イベントログ設計.md) §3 に整理済み）。

→ **repoのSQL＝デプロイ済みスキーマ ではない**。一部はSupabase上で直接適用された。`05_Database_Schema.md` を実DB基準で更新する必要がある。

---

## 5. scripts/ 棚卸し

| 分類 | 内容 | 扱い |
|------|------|------|
| 稼働（参照あり） | `update-version.mjs`（package.json `prebuild`/`version:update`） | 残す |
| 一回限り調査（過去インシデント） | `investigate-*` / `find-*-duplicates` / `analyze-1019-7766-*` / `check-1019-7766-*` / `check-kakiya-*` / `investigate-15point-*` 等 | 多数。アーカイブ/削除候補 |
| テスト | `test-*.mjs` | 必要分のみ残す |
| 確認系 | `check-*.mjs`（schema/rls/rewards等） | 汎用は残す・特定ユーザー向けは削除候補 |
| 今回追加 | `check-user-8305-logs.mjs` / `check-manual-adjust-records.mjs` / `check-8305-scan-failure.mjs` / `introspect-readonly.mjs` | 調査用。要否を判断 |

※ `scripts/` 内に `check-event-logs.mjs` のように **anonでINSERTテストを行う**スクリプトがある。運用DBに対しては実行注意（テストデータが残る可能性）。

---

## 6. 推奨アクション（変更は別途承認の上で）

| # | 対象 | 推奨 | リスク |
|---|------|------|--------|
| 1 | `05_Database_Schema.md` | 実DB（§4）基準で全面更新 | なし（doc） |
| 2 | `02_ファイル構成.md` | 未使用ファイル（ToothDiagram v1, KidsSlotButton）に注記 | なし（doc） |
| 3 | マイグレーション | 欠番(026/030)と直接作成テーブルの経緯を記録（理想はrepoに反映） | 低（記録のみ） |
| 4 | スタンプ付与3系統 | 内部関数へ集約（段階的） | 中（運用中・要テスト） |
| 5 | 未使用ルート/コンポーネント | 削除（rewards GET, members bare, ToothDiagram v1, KidsSlotButton） | 低（未参照確認済み） |
| 6 | デッドコード(analytics) | 実装 or 削除の判断 | 低 |

> 本レポートは「実態の記録」が目的。コード/DB変更は行っていない。削除・集約は個別に承認・テストの上で実施すること。

---

## 改訂履歴
| 日付 | 内容 |
|------|------|
| 2026-06-12 | 初版作成（全コード俯瞰 + Supabase読み取り専用実態調査） |
