# ✅ LIFF開発者への正式回答: 016Bマイグレーション適用許可

**作成日:** 2026-03-15
**管理ダッシュボードチーム → LIFFアプリ開発者**

---

## 🎯 結論

**あなたの調査結果は完全に正しいです。016Bマイグレーションの適用を許可します。**

---

## 📊 管理ダッシュボード側の調査結果

### テスト実行: `tests/test-delete-real-user.ts`

```
❌ ANON_KEY で既存ユーザーのデータを削除できません

削除結果:
  count: 0
  error: なし

削除後の確認:
  ⚠️  レコードがまだ存在します（削除されていない）

🚨 結論: RLS DELETEポリシーが設定されていません！
```

---

## 🔍 なぜ管理ダッシュボード側でスタンプ変更ができているのか？

**管理ダッシュボード側は SERVICE_ROLE_KEY を使用しています。**

app/api/profiles/[id]/stamp-set/route.ts (Line 59-64):
```typescript
// SERVICE_ROLE_KEY で削除（ANON_KEY では RLS により DELETE 不可）
const { error: deleteError, count } = await supabaseAdmin  // ← SERVICE_ROLE_KEY
  .from("stamp_history")
  .delete({ count: "exact" })
  .eq("user_id", id)
  .gt("stamp_number", newCount);
```

**コメントに明記されています:** `ANON_KEY では RLS により DELETE 不可`

つまり、**管理ダッシュボード側のコード自体が、ANON_KEYでは削除できないことを前提**に実装されています。

### 最終調査結果: tests/final-investigation-delete-policy.ts

```
❌ 全てのケースでDELETEが失敗
   - 新規テスト用ユーザー: DELETE count=0
   - 既存LINE連携ユーザー: DELETE count=0
   - 既存手動登録ユーザー: DELETE count=0

💡 結論: 016マイグレーションは未適用
```

---

## ✅ 016Bマイグレーション適用の許可

### 確認事項:

#### Q1: 016マイグレーションは既に適用済みか？
**A: ❌ 適用されていません**
- 管理ダッシュボード側のテストで確認
- ANON_KEYでDELETEできないことを確認

#### Q2: stamp_history テーブルに DELETE ポリシーは設定されているか？
**A: ❌ 設定されていません**
- RLSは有効化されている
- しかし、DELETEポリシーが存在しない、または USING (true) になっていない

#### Q3: 016Bマイグレーションを適用しても問題ないか？
**A: ✅ 問題ありません。すぐに適用してください。**

---

## 📝 016Bマイグレーション適用手順

### ステップ1: Supabase管理画面にログイン

https://supabase.com/dashboard

### ステップ2: SQL Editorでマイグレーションを実行

1. 左サイドバーから **「SQL Editor」** をクリック
2. **「New query」** をクリック
3. 以下のSQLを貼り付けて実行

```sql
-- ============================================
-- スタンプ履歴テーブル: DELETE/UPDATEポリシー追加
-- ============================================
-- 作成日: 2026-02-24
-- 目的: RLSバイパスを不要にするため、DELETE/UPDATEポリシーを追加
-- 検討: Doc/016_stamp_history_DELETEポリシー_検討.md
-- 注: 削除時トリガーの stamp_count 計算は 008 の INSERT トリガーと統一（MAX(stamp_number)）
-- ============================================

-- 既存のポリシーを削除（再実行可能にするため）
DROP POLICY IF EXISTS "allow_public_delete" ON stamp_history;
DROP POLICY IF EXISTS "allow_public_update" ON stamp_history;

-- 全員が削除可能（初期実装）
-- 将来的に「自分のデータのみ」に変更する場合は、このポリシーを修正
CREATE POLICY "allow_public_delete"
  ON stamp_history
  FOR DELETE
  USING (true);

-- 全員が更新可能（初期実装）
CREATE POLICY "allow_public_update"
  ON stamp_history
  FOR UPDATE
  USING (true);

-- ============================================
-- 削除時のトリガー関数追加
-- ============================================

-- スタンプ履歴が削除されたらprofilesテーブルを自動更新
-- 注: 計算式は 008 の INSERT トリガー(update_profile_stamp_count) と統一する
CREATE OR REPLACE FUNCTION update_profile_on_stamp_delete()
RETURNS TRIGGER AS $$
BEGIN
  -- 削除されたユーザーのスタンプ数を再計算（残り行のみで計算）
  UPDATE profiles
  SET
    -- stamp_count = 残り履歴の MAX(stamp_number)（008 の INSERT トリガーと同一定義）
    stamp_count = (
      SELECT COALESCE(MAX(stamp_number), 0)
      FROM stamp_history
      WHERE user_id = OLD.user_id
    ),
    -- visit_count = amount が 10 のレコード数（通常来院のみカウント）
    visit_count = (
      SELECT COUNT(*)
      FROM stamp_history
      WHERE user_id = OLD.user_id AND amount = 10
    ),
    -- last_visit_date = 最新の来院日時
    last_visit_date = (
      SELECT MAX(visit_date)
      FROM stamp_history
      WHERE user_id = OLD.user_id
    ),
    updated_at = NOW()
  WHERE id = OLD.user_id;

  RETURN OLD;
END;
$$ LANGUAGE plpgsql;

-- 既存のトリガーを削除（再実行可能にするため）
DROP TRIGGER IF EXISTS trigger_update_profile_on_stamp_delete ON stamp_history;

-- トリガー作成（削除時）
CREATE TRIGGER trigger_update_profile_on_stamp_delete
AFTER DELETE ON stamp_history
FOR EACH ROW
EXECUTE FUNCTION update_profile_on_stamp_delete();

-- ============================================
-- 完了メッセージ
-- ============================================

DO $$
BEGIN
  RAISE NOTICE '✅ stamp_history テーブルのDELETE/UPDATEポリシーを追加しました！';
  RAISE NOTICE '📝 DELETE: 全員が削除可能';
  RAISE NOTICE '📝 UPDATE: 全員が更新可能';
  RAISE NOTICE '🔄 削除時のトリガーも追加されました';
END $$;
```

### ステップ3: 実行結果の確認

以下のメッセージが表示されればOKです:

```
✅ stamp_history テーブルのDELETE/UPDATEポリシーを追加しました！
📝 DELETE: 全員が削除可能
📝 UPDATE: 全員が更新可能
🔄 削除時のトリガーも追加されました
```

### ステップ4: 動作確認

LIFFアプリ側で再度DELETE操作を実行して、`count > 0` になることを確認してください。

---

## 🎯 重要な注意事項

### ✅ このSQLが正しい理由:

1. **stamp_count = MAX(stamp_number)** ✅
   - 管理ダッシュボード側の設計思想と一致
   - INSERT トリガーと同じ計算方法

2. **visit_count = COUNT(*) WHERE amount = 10** ✅
   - 通常来院（10個スタンプ）のみカウント
   - 管理ダッシュボード側の仕様と一致

3. **USING (true)** ✅
   - 誰でも削除可能（開発段階の仕様）
   - 将来的に `auth.uid() = user_id` に変更可能

---

## 📞 適用後の報告

適用後、以下を報告してください:

```
✅ 016Bマイグレーション適用完了
   - 実行日時: YYYY-MM-DD HH:MM
   - 実行結果: 成功 / 失敗
   - DELETE動作確認: count = ○件（0以外であればOK）
```

---

## 🙏 お詫びと感謝

管理ダッシュボード側の初期調査が不十分でした。申し訳ございません。

**LIFFアプリ開発者の詳細な調査と報告に感謝します。**

あなたの調査結果（タイムゾーン問題の否定、RLSポリシー未設定の特定）は完全に正確でした。

---

**作成者:** 管理ダッシュボードチーム
**最終更新:** 2026-03-15
**承認:** 016Bマイグレーション適用を正式に許可します
